name: Contributors

on:
  schedule:
    - cron: '3 4 1 * *'

  workflow_dispatch:

permissions:
  contents: read

jobs:
  contributors:
    permissions:
      pull-requests: write
      contents: write
    uses: go-openapi/ci-workflows/.github/workflows/contributors.yml@d2d2b54984441dbea93c5672174cd949c60b4fc5 # v0.5.3
    secrets: inherit
